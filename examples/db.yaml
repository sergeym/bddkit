paths: [examples/db-features]

default_db: default

resources:
  api:
    stub:
      base_url: http://127.0.0.1:1

  db:
    default:
      dsn: postgres://postgres:postgres@127.0.0.1:5433/postgres
      search_path: [apibdd_demo]
    reporting:
      dsn: postgres://postgres:postgres@127.0.0.1:5433/postgres
      search_path: [apibdd_demo_reporting]
